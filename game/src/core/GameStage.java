package core;import Graphic.TileMap;import javafx.scene.Group;import javafx.scene.Scene;import javafx.stage.Stage;import javafx.scene.canvas.Canvas;import java.util.ArrayList;/*|=======================================================================||   This class start a game field (game field's status, config, ...)    ||=======================================================================| */public class GameStage {    static class Map {        TileMap background = new TileMap(); //first layer        TileMap road = new TileMap();       //second layer        TileMap rockTree = new TileMap();   //third layer        TileMap spawn = new TileMap();      //final layer        public Map(){}    }    public static void generateWindow(Stage primaryStage){        //Parent group        Group root = new Group();        Scene theScene = new Scene(root);        Canvas canvas = new Canvas(Config.SCREEN_WIDTH, Config.SCREEN_HEIGHT);        root.getChildren().add(canvas);        primaryStage.setScene(theScene);        primaryStage.setTitle(Config.TITLE);        //need to be inside class Render, manually add latter        primaryStage.show();    }    public static void loadMap(){        Map mapData = new Map();        //input layer data        mapData.background.readFile("Map/background.txt");        mapData.road.readFile("Map/road.txt");        mapData.rockTree.readFile("Map/rockTree.txt");        mapData.spawn.readFile("Map/spawn.txt");        //print to check        mapData.background.printMapData();        mapData.road.printMapData();        mapData.rockTree.printMapData();        mapData.spawn.printMapData();    }    public static void loadGameplay(){    }    public static void closeWindow(Stage primaryStage){        primaryStage.close();        System.out.println("Window closed");    }}