package Graphic;import core.Config;import javafx.scene.Group;import javafx.scene.Scene;import javafx.scene.canvas.Canvas;import javafx.scene.image.Image;import javafx.scene.image.ImageView;import javafx.scene.layout.FlowPane;import javafx.stage.Stage;public class Render {    public Render(){ }    public static void renderMap() {    }    public static void renderMap(int[][] tileMap, ImageSheet imageSheet){        Stage primaryStage = new Stage();        //Parent group        Group root = new Group();        Scene theScene = new Scene(root);        Canvas canvas = new Canvas(Config.SCREEN_WIDTH, Config.SCREEN_HEIGHT);        root.getChildren().add(canvas);        ImageView[][] imageView = new ImageView[Config.SCREEN_HEIGHT][Config.SCREEN_WIDTH];        for (int i = 0; i < Config.SCREEN_HEIGHT; i++) {            for (int j = 0; j < Config.SCREEN_WIDTH; j++) {                imageView[i][j]= new ImageView(imageSheet.imageSheet.get(tileMap[i][j]));                imageView[i][j].setX(i*64);                imageView[i][j].setY(j*64);                root.getChildren().addAll(imageView[i][j]);            }        }        primaryStage.setScene(theScene);        primaryStage.setTitle(Config.TITLE);        //need to be inside class Render, manually add latter        primaryStage.show();    }}